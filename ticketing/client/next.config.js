/** @type {import('next').NextConfig} */
const nextConfig = {}

// to be added later with nextjs does not responds properly
// webpack: (config) => {
//     config.watchOptions.poll = 300;
//     return config;
//   },

module.exports = nextConfig
