name: auth-tests

on: 
  push:
    branches:
      - 'main'
    paths:
      - 'ticketing/auth/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run tests
        run: |
          cd ticketing/auth
          npm install
          npm run test:ci